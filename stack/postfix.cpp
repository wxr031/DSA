#include <iostream>
#include <string>
#include <cctype>
#include "int_stack.h"
using namespace std;
int main() {
	Stack stack;
	string exp;
	while (getline(cin, exp)) {
		for (char c: exp) {
			if (isdigit(c))
		}
	}
	return 0;
}
