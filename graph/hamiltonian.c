#include <stdio.h>
#include <stdlib.h>



int main() {
	int vertex, edge;
	scanf("%d%d", &vertex, &edge);
	int **adj = (int **)malloc(vertex * sizeof(int));
	
	for(int i = 0, c1, c2; i < edge; ++i) {
		scanf("%d%d", &c1, &c2);

	}
}
